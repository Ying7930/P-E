void setup(){
  size(500, 515);
  background(0, 0, 0);
}

void draw() {
  int y=0;
  while (y<470) {
    int x=0;
    while (x<500) {
      ellipse(20+y, 20+x, 20, 20);
      x+=22;
    }
    y+=22;
  }
}
